import React from "react"

export default function Home() {
  return <div><p>Gatsby looks for the pages folder to create corresponding routes</p>
  <p>Any default export component that you have inside of your source slash pages folder is going to have a route automatically created for it</p>
  </div>
}
